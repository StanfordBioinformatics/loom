fixed_inputs:
- channel: fib0
  data:
    contents: '0'
  type: string
- channel: fib1
  data:
    contents: '1'
  type: string
name: fib
outputs:
- channel: fib11
  type: string
steps:
- command: echo $(({{fib1}}+{{fib0}}))
  environment:
    docker_image: ubuntu
  inputs:
  - channel: fib1
    type: string
  - channel: fib0
    type: string
  name: fib2
  outputs:
  - channel: fib2
    source:
      stream: stdout
    type: string
  resources:
    cores: '1'
    memory: '1'
- command: echo $(({{fib2}}+{{fib1}}))
  environment:
    docker_image: ubuntu
  inputs:
  - channel: fib2
    type: string
  - channel: fib1
    type: string
  name: fib3
  outputs:
  - channel: fib3
    source:
      stream: stdout
    type: string
  resources:
    cores: '1'
    memory: '1'
- command: echo $(({{fib3}}+{{fib2}}))
  environment:
    docker_image: ubuntu
  inputs:
  - channel: fib3
    type: string
  - channel: fib2
    type: string
  name: fib4
  outputs:
  - channel: fib4
    source:
      stream: stdout
    type: string
  resources:
    cores: '1'
    memory: '1'
- command: echo $(({{fib4}}+{{fib3}}))
  environment:
    docker_image: ubuntu
  inputs:
  - channel: fib4
    type: string
  - channel: fib3
    type: string
  name: fib5
  outputs:
  - channel: fib5
    source:
      stream: stdout
    type: string
  resources:
    cores: '1'
    memory: '1'
- command: echo $(({{fib5}}+{{fib4}}))
  environment:
    docker_image: ubuntu
  inputs:
  - channel: fib5
    type: string
  - channel: fib4
    type: string
  name: fib6
  outputs:
  - channel: fib6
    source:
      stream: stdout
    type: string
  resources:
    cores: '1'
    memory: '1'
- command: echo $(({{fib6}}+{{fib5}}))
  environment:
    docker_image: ubuntu
  inputs:
  - channel: fib6
    type: string
  - channel: fib5
    type: string
  name: fib7
  outputs:
  - channel: fib7
    source:
      stream: stdout
    type: string
  resources:
    cores: '1'
    memory: '1'
- command: echo $(({{fib7}}+{{fib6}}))
  environment:
    docker_image: ubuntu
  inputs:
  - channel: fib7
    type: string
  - channel: fib6
    type: string
  name: fib8
  outputs:
  - channel: fib8
    source:
      stream: stdout
    type: string
  resources:
    cores: '1'
    memory: '1'
- command: echo $(({{fib8}}+{{fib7}}))
  environment:
    docker_image: ubuntu
  inputs:
  - channel: fib8
    type: string
  - channel: fib7
    type: string
  name: fib9
  outputs:
  - channel: fib9
    source:
      stream: stdout
    type: string
  resources:
    cores: '1'
    memory: '1'
- command: echo $(({{fib9}}+{{fib8}}))
  environment:
    docker_image: ubuntu
  inputs:
  - channel: fib9
    type: string
  - channel: fib8
    type: string
  name: fib10
  outputs:
  - channel: fib10
    source:
      stream: stdout
    type: string
  resources:
    cores: '1'
    memory: '1'
- command: echo $(({{fib10}}+{{fib9}}))
  environment:
    docker_image: ubuntu
  inputs:
  - channel: fib10
    type: string
  - channel: fib9
    type: string
  name: fib11
  outputs:
  - channel: fib11
    source:
      stream: stdout
    type: string
  resources:
    cores: '1'
    memory: '1'
